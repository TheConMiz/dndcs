import React, { Fragment } from 'react';

import { Paper } from '@material-ui/core';

export const Glance = () => {
    return (
        <Fragment>
            <Paper
                className={classes.paper}
                variant="outlined"
            >

            </Paper>
        </Fragment>
    )
}
